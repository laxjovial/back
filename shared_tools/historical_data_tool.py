import yfinance as yf
import pandas as pd

class HistoricalDataTool:
    def get_historical_stock_data(self, symbol: str, start_date: str, end_date: str) -> pd.DataFrame:
        """
        Gets historical stock data for a given symbol and date range.

        :param symbol: The stock symbol.
        :param start_date: The start date in YYYY-MM-DD format.
        :param end_date: The end date in YYYY-MM-DD format.
        :return: A DataFrame with the historical stock data.
        """
        # ... (rest of the function code)
        pass
